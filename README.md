# go_snippet

The `c_parse.go` file can parse a c file and find all the function defintion in a file. This can be use full to make a call graph. The next would be to the same. Will take some open source project which is written in C and run the file through it.

Note: Not sure if this is good idea to chunk it like this but let's learn from the mistakes.
